export enum SUIT {
    SPADE = 0,
    HEART,
    CLUB,
    DIAMOND
}

export enum GAME_STATUS {
    NOT_READY,
    READY,
    BLIND_BID,
    BID,
    BOOK,
    BOOK_OVER,
    ROUND_OVER,
    GAME_OVER,
}